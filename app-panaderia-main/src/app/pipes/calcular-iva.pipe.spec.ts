import { CalcularIvaPipe } from './calcular-iva.pipe';

describe('CalcularIvaPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcularIvaPipe();
    expect(pipe).toBeTruthy();
  });
});
